npx lint-staged
npm run typecheck
npm run test
if command -v gitleaks >/dev/null 2>&1; then
  gitleaks protect --staged --no-banner
else
  echo "warning: gitleaks not installed locally - skipping secret scan (CI still scans)"
fi
